---
created: 2026-06-14
type: contract
contract: skill-schema
version: 1
tags: [book2skill, skill-distillation, contract]
---

# Contract · skill（file）v1

> [[abstract-to-skill]] 产出、[[promote-to-registry]] 治理的单位。grammar = **reasoning / decision** 两种原始动作。

## frontmatter
`name`（动词开头、内容无关）· `kind`（reasoning|decision）· `one_liner` · `source_*`（book / run / anchors）· `perspective_origin`

## sections
When to use / Method（可迁移步骤·框架·判据）/ Inputs → Outputs / Failure modes · assumptions / Worked example（optional，单个，明确标注 —— **实例唯一允许出现处**）

## 🔥 RAG firewall（validity 判据）
不指名具体例子就讲不出来 = **无效**（是 RAG 不是 skill）。例子只允许在 worked-example 槽；promote 时连品牌名也清掉。
- 已知 leak 形态：把公司当 **reference-class 标签**偷渡进 Method（v2 要抓）。
