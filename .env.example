# Copy to .env and fill in. The eval talks to any OpenAI-compatible endpoint.
BOOK2SKILL_LLM_BASE_URL=http://localhost:8000/v1
BOOK2SKILL_LLM_MODEL=your-model-id
# BOOK2SKILL_LLM_API_KEY=sk-...           # if your endpoint requires a key
# BOOK2SKILL_LLM_PROXY=socks5h://localhost:1055   # only if the endpoint is behind a tunnel
