# third-party benchmark (clone separately; not redistributed)
PolyBench/

# python env
.venv/
__pycache__/
*.pyc

# fetched market data + run outputs (regenerable)
data/
results/

# extraction runs: contain raw extracted BOOK TEXT (copyrighted) + internal provenance — never publish
runs/

# local secrets / endpoint config
.env

# os
.DS_Store
